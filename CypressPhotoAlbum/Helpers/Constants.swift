import Foundation

enum Constants {
    static let baseUrl = "https://jsonplaceholder.typicode.com"
    static let photoUrl = "\(baseUrl)/photos"
    static let albumUrl = "\(baseUrl)/albums"
    static let AlbumRowID = "AlbumRow"
    static let HorizontalCellID = "HorizontalCell"

}
